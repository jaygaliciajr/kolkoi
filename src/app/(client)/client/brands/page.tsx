import { ClientSectionShell } from "@/components/client/ClientSectionShell";

export default function ClientBrandsPage() {
  return (
    <ClientSectionShell
      title="Brands"
      subtitle="Manage your brand portfolio, campaign ownership, and default delivery preferences."
      bodyTitle="Brand records"
      bodyDescription="Multi-brand support is now part of the V2 data model foundation."
    />
  );
}
