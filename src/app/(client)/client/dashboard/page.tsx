import { ClientSectionShell } from "@/components/client/ClientSectionShell";

export default function ClientDashboardPage() {
  return (
    <ClientSectionShell
      title="Client Dashboard"
      subtitle="Track campaign requests, active delivery, and manager-led execution for your brands."
      bodyTitle="V2 Client workspace enabled"
      bodyDescription="Manager-mediated workflow is active. Direct client-to-influencer communication remains blocked by design."
    />
  );
}
